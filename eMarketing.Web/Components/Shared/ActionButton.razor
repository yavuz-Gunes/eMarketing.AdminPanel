<button class="@CssClass" @onclick="OnClick">
    <span class="@IconClass"><AppIcon Name="@Icon" CssClass="h-5 w-5" /></span>
    <span class="flex-1 text-left">@Title</span>
    <AppIcon Name="chevron-right" CssClass="h-5 w-5 text-slate-400" />
</button>

@code {
    [Parameter] public string Icon { get; set; } = "circle";
    [Parameter] public string Title { get; set; } = string.Empty;
    [Parameter] public string Tone { get; set; } = "blue";
    [Parameter] public EventCallback OnClick { get; set; }

    private string CssClass => Tone == "danger"
        ? "flex w-full items-center gap-3 rounded-xl border border-rose-200 bg-white p-4 font-semibold text-rose-700 shadow-sm transition hover:bg-rose-50"
        : "flex w-full items-center gap-3 rounded-xl border border-slate-200 bg-white p-4 font-semibold text-slate-900 shadow-sm transition hover:border-blue-200 hover:bg-slate-50";

    private string IconClass => Tone switch
    {
        "danger" => "flex h-10 w-10 items-center justify-center rounded-xl bg-rose-50 text-rose-600",
        "amber" => "flex h-10 w-10 items-center justify-center rounded-xl bg-amber-50 text-amber-600",
        _ => "flex h-10 w-10 items-center justify-center rounded-xl bg-blue-50 text-blue-600"
    };
}
